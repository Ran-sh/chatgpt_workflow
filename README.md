# ChatGPT Workflow

Agent Handoff Protocol mother template.

Purpose:
- Provide reusable AI agent collaboration workflow.
- Keep task ownership explicit through ACTIVE task files.
- Make GitHub the source of truth for tasks, results and commits.

## Usage

A project imports this workflow, adapts it to its own stack, and keeps its own generated copy.

Source template:
- templates/

No project-specific business rules belong here.
