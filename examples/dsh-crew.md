# dsh-crew Reference Example

Role:
- Agent orchestration/platform reference.

Uses Agent Workflow for:
- coordinating execution;
- managing agent state;
- validating multi-agent workflows.

Important:
- Orchestration does not replace the task contract.
- Any execution engine can consume the same task.

Source template:
- Ran-sh/chatgpt_workflow
