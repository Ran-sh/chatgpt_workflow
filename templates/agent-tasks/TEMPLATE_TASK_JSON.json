{
  "$schema": "../schema/task-contract.schema.json",
  "task_id": "TASK-YYYYMMDD-001",
  "mode": "IMPLEMENT",
  "title": "Replace with concrete task title",
  "objective": "Describe the outcome, not the implementation.",
  "scope": {
    "allowed_paths": [],
    "forbidden_paths": []
  },
  "constraints": [
    "Follow repository policy.",
    "Do not invent commands or requirements."
  ],
  "validation": {
    "required_checks": []
  },
  "executor": {
    "platform_neutral": true
  }
}
