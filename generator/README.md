# Workflow Generator

The generator layer converts the generic Agent Workflow Protocol into a project-specific installation.

Goals:

- Detect project stack.
- Generate local workflow files.
- Preserve business code isolation.
- Create machine-readable task contracts.

The generator must not modify application logic during installation.
